services:
  servidor:
    build: ./servidor
    # Roda a versão concorrente
    command: run --allow-net concorrente.ts
    ports:
      - "5000:5000"

  cliente:
    build: ./cliente
    environment:
      SERVER_HOST: "servidor"
    depends_on:
      - servidor
    deploy:
      replicas: 10